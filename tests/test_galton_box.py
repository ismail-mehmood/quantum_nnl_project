# This program should take in a bin_counts dict, a bias, and compute classically
# the expected distribution. It will then compute a regression statistic and 
# report TRUE/FALSE

def test_galton_box(bin_counts, n):
    return False